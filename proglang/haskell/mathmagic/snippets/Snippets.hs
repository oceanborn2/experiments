if True then True else False

--let msg=intercalate(["sum:", (show sm1), " = ", (show sm2), " /= ", (show dif)])
let msg=concat(map (show)([1, 2, 3, 4, 6])) --"sum:", (show sm1), " = ", (show sm2), " /= ", (show dif)])
